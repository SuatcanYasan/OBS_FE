import React, {useEffect, useState} from "react"import "devextreme/dist/css/dx.light.css"import PropTypes from "prop-types"import {Link, useLocation, withRouter} from "react-router-dom"import {withTranslation} from "react-i18next"import {AccordionBody, AccordionHeader, AccordionItem, Col, Container, Row, UncontrolledAccordion} from "reactstrap"import CustomBreadcrumbs from "../../components/breadcrumbs"import {completeClassesService, getAllClassesService, getClassesInfoService} from "../../services/class/classService";import Courses from "./components/courses";import ClassInfo from "./components/class_info";const ClassPage = props => {    const location = useLocation();    let params = new URLSearchParams(location.search)    const [classInfo, setClassInfo] = useState({})    useEffect(() => {        getAllClassesService(params.get("id")).then(response => {            setClassInfo(response)        })    }, [params.get("id")]);    return (        <React.Fragment>            <div className="page-content">                <Container fluid className="dash">                    <div>                        <CustomBreadcrumbs                            breadcrumbItems={[{title: classInfo?.class_name, url: "/classes"}]}/>                    </div>                    <div className="mt-2">                        <Row>                            <Courses/>                            <ClassInfo classInfo={classInfo}/>                        </Row>                    </div>                </Container>            </div>        </React.Fragment>    )        ;}ClassPage.propTypes = {    location: PropTypes.any,    t: PropTypes.any,};export default withRouter(withTranslation()(ClassPage));